# Future extensions

- Project files with multi-workspace metadata, language runtime selection, dependencies and thumbnails.
- Per-language validation/diagnostics before source generation and execution.
- Translation assistant that offers supported semantic mappings and explicit loss reports.
- Block package marketplace with signed/versioned manifests.
- Offline/local runner only after sandboxing and quota design.
- Collaboration/history through a workspace-operation layer rather than raw XML synchronization.
- Telemetry limited to consented, privacy-safe product diagnostics.
